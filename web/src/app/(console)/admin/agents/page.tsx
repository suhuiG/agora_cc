import { AgentToolMatrix } from "@/components/admin/agents/AgentToolMatrix";

// Agent × Tool 매트릭스 — 콘솔의 핵심 화면 (S3: 읽기 + 켜기).
export default function AdminAgentsPage() {
  return <AgentToolMatrix />;
}
