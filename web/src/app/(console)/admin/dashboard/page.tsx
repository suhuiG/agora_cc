import { DashboardClient } from "@/components/admin/dashboard/DashboardClient";

// 대시보드 (§3-M5) — 거버넌스 관측(통과율·위험/등급 분포·SLA).
export default function AdminDashboardPage() {
  return <DashboardClient />;
}
